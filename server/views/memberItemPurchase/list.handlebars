{{#extend 'styles'}}
    <style>
        .error {
            color: red;
        }
        .success {
            color:green;
        }
    </style>
{{/extend}}
<!-- Content Header (Page header) -->
<section class="content-header">
    <h1>
        List Member Item Purchase
        <small>All Member Item Purchase listed</small>
    </h1>
    <ol class="breadcrumb">
        <li><a href="{{{url 'home'}}}"><i class="fa fa-home"></i> Home</a></li>
        <li class="active">List</li>
    </ol>
</section>
<!-- Main content -->
<section class="content">
    <div class="row">
        <div class="col-xs-12">
            {{#if message}}
                {{{message}}}
            {{/if}}
            <div class="box">
                <div class="box-header">
                    <h3 class="box-title">All Member Item Purchase</h3>
                    <div class="box-tools">
                        <div class="input-group input-group-sm" style="width: 150px;">
                            <input type="text" id="table-search" class="form-control pull-right" placeholder="Search">

                            <div class="input-group-btn">
                                <button type="button" class="btn btn-default search"><i class="fa fa-search"></i></button>
                            </div>
                        </div>
                    </div>
                </div>
                <!-- /.box-header -->
                <div class="box-body table-responsive no-padding">
                    <table class="table table-hover">
                        <tr>
                            <th>MemberItemPurchaseID</th>
                            <th>MemberID</th>
                            <th>ItemListID</th>
                            <th>PurchasePrice</th>
                            <th>PurchaseQuantity</th>
                            <th>CreatedAt</th>
                        </tr>
                        {{#each listObjs }}
                            <tr>
                                <td>
                                    <a href="/purchase/{{this.MemberItemPurchaseID}}">
                                        {{this.MemberItemPurchaseID}}
                                    </a>
                                </td>
                                <td><a href="/member/{{this.MemberID}}"> {{this.MemberID}} </a></td>
                                <td><a href="/itemList/{{this.ItemListID}}"> {{this.ItemListID}}</a> </td>
                                <td> {{this.PurchasePrice}} </td>
                                <td> {{this.PurchaseQuantity}} </td>
                                <td>{{this.CreatedAt}}</td>
                            </tr>
                        {{/each}}
                    </table>
                </div>
                <!-- /.box-body -->
                <div class="box-footer">
                    <button type="button" class="btn btn-primary" onclick="location.href = '{{{url "memberItemPurchase.create"}}}'">Create Member Item Purchase</button>
                </div>
            </div>
            <!-- /.box -->
        </div>
    </div>
    <!-- /.row -->
</section>
