{{#extend 'styles'}}
    <style>
        .error {
            color: red;
        }
        .success {
            color:green;
        }
    </style>
{{/extend}}
<!-- Content Header (Page header) -->
<section class="content-header">
    <h1>
        List GiftDepository
        <small>All GiftDepository listed</small>
    </h1>
    <ol class="breadcrumb">
        <li><a href="{{{url 'home'}}}"><i class="fa fa-home"></i> Home</a></li>
        <li class="active">List</li>
    </ol>
</section>
<!-- Main content -->
<section class="content">
    <div class="row">
        <div class="col-xs-12">
            {{#if message}}
                {{{message}}}
            {{/if}}
            <div class="box">
                <form method="get">
                    <div class="box-header">
                        <h3 class="box-title">All gift depository</h3>
                        <div class="box-tools">
                            <div class="input-group input-group-sm" style="width: 150px;">
                                <input type="text" id="table-search" name="keyword" class="form-control pull-right" placeholder="Search" value="{{ keyword}}">

                                <div class="input-group-btn">
                                    <button type="button" class="btn btn-default search"><i class="fa fa-search"></i></button>
                                </div>
                            </div>
                        </div>
                    </div>
                </form>
                <!-- /.box-header -->
                <div class="box-body table-responsive no-padding">
                    <table class="table table-hover">
                        <tr>
                            <th>GiftDepositoryID</th>
                            <th>ItemListID</th>
                            <th>ItemCount</th>
                            <th>FromMemberID</th>
                            <th>ToMemberID</th>
                            <th>CreatedAt</th>
                        </tr>
                        {{#each listObjs }}
                            <tr>
                                <td>
                                    <a href="/giftDepository/{{this.GiftDepositoryID}}"> {{this.GiftDepositoryID}} </a>
                                </td>
                                <td> {{this.ItemListID}} </td>
                                <td> {{this.ItemCount}} </td>
                                <td>
                                    <a href="/member/{{this.FromMemberID}}"> {{this.FromMemberID}} </a>
                                </td>
                                <td>
                                    <a href="/member/{{this.ToMemberID}}"> {{this.ToMemberID}} </a>
                                </td>
                                <td>{{formatTimeDate this.createdAt}}</td>
                            </tr>
                        {{/each}}
                    </table>
                </div>
                <!-- /.box-body -->
                <div class="box-footer">
                    <ul class="pagination pagination-sm no-margin pull-right">
                        {{{ pagination page count keyword}}}
                    </ul>
                    <button type="button" class="btn btn-primary" onclick="location.href = '{{{url "giftDepository.create"}}}'">Create Gift Depository</button>
                </div>
            </div>
            <!-- /.box -->
        </div>
    </div>
    <!-- /.row -->
</section>
