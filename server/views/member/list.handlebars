{{#extend 'styles'}}
    <style>
        .error {
            color: red;
        }
        .success {
            color:green;
        }
    </style>
{{/extend}}
<!-- Content Header (Page header) -->
<section class="content-header">
    <h1>
        List Member
        <small>All member listed</small>
    </h1>
    <ol class="breadcrumb">
        <li><a href="{{{url 'home'}}}"><i class="fa fa-home"></i> Home</a></li>
        <li class="active">List</li>
    </ol>
</section>
<!-- Main content -->
<section class="content">
    <div class="row">
        <div class="col-xs-12">
            {{#if message}}
                {{{message}}}
            {{/if}}
            <div class="box">
                <div class="box-header">
                    <h3 class="box-title">All member</h3>
                    <div class="box-tools">
                        <div class="input-group input-group-sm" style="width: 150px;">
                            <input type="text" id="table-search" class="form-control pull-right" placeholder="Search">

                            <div class="input-group-btn">
                                <button type="button" class="btn btn-default search"><i class="fa fa-search"></i></button>
                            </div>
                        </div>
                    </div>
                </div>
                <!-- /.box-header -->
                <div class="box-body table-responsive no-padding">
                    <table class="table table-hover">
                        <tr>
                            <th>MemberID</th>
                            <th>Email</th>
                            <th>UserName</th>
                            <th>MemberGroup</th>
                            <th>LastDeviceID</th>
                            <th>PushNotificationID</th>
                            <th>CreatedAt</th>
                        </tr>
                        {{#each listObjs }}
                            <tr>
                                <td>
                                    <a href="/member/{{this.MemberID}}">
                                        {{this.MemberID}}
                                    </a>
                                </td>
                                <td>
                                    {{this.EmailAddress}}
                                </td>
                                <td>{{this.Name1}} </td>
                                <td>{{this.MemberGroup}} </td>
                                <td>{{this.LastDeviceID}}</td>
                                <td>{{this.PushNotificationId}}</td>
                                <td>{{this.createdAt}}</td>
                            </tr>
                        {{/each}}
                    </table>
                </div>
                <div class="box-footer clearfix">
                    <ul class="pagination pagination-sm no-margin pull-right">
                        {{{ pagination page count }}}
                    </ul>
                    <button type="button" class="btn btn-primary" onclick="location.href = '{{{url "member.create"}}}'">Create New Member</button>
                </div>
            </div>
            <!-- /.box -->
        </div>
    </div>
    <!-- /.row -->
</section>
